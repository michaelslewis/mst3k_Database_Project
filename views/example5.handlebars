{{> header}}

<div class="container-fluid text-center">
  <h1>Display the genre and subgenre names for any movies directed by Roger Corman:</h1>
  <br>
</div>
<div class="container-fluid col-md-offset-3">
  <div class="codeExample">
    <code>SELECT DISTINCT genre.genre_name, genre.subgenre_name FROM genre</code>
    <br>
    <code>INNER JOIN movie ON genre.id = movie.genre_id</code>
    <br>
    <code>WHERE movie.director_fname = "Roger" AND movie.director_lname = "Corman"</code>
  </div>
</div>
<br>

{{> displayResults}}
