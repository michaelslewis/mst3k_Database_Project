{{> header}}

<div class="container-fluid text-center">
  <h1>Display the first and last names of any actor who voiced the character of 'Tom Servo':</h1>
  <br>
</div>
<div class="container-fluid col-md-offset-3">
  <div class="codeExample">
  	<code>SELECT actor.fname, actor.lname FROM actor</code>
  	<br>
    <code>INNER JOIN actor_character ON actor.id = actor_character.actor_id</code>
    <br>
    <code>INNER JOIN mst3k_character ON mst3k_character.id = actor_character.character_id</code>
    <br>
    <code>WHERE mst3k_character.fname = "Tom" AND mst3k_character.lname = "Servo"</code>
  </div>
</div>
<br>

{{> displayResults}}
